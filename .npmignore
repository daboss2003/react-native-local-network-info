# Dev-only noise (the package.json "files" allowlist is the primary control).
example/
node_modules/
.expo/
*.log
*.tgz
.DS_Store

# Generated native build output — must never be published.
android/build/
android/.gradle/
android/.cxx/
ios/build/
ios/Pods/
**/*.xcworkspace

# Keep build/, android/ + ios/ source, src/ and expo-module.config.json.
